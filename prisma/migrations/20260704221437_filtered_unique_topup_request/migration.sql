BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[transactions] DROP CONSTRAINT [transactions_topUpRequestId_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
