BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[transactions] ADD [proofImageUrl] NVARCHAR(500),
[senderPhone] VARCHAR(20);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
