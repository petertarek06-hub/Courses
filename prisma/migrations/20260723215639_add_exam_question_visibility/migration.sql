BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[exam_questions] ADD [isVisible] BIT NOT NULL CONSTRAINT [exam_questions_isVisible_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
